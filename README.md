# Text-Summarization-using-LLM
The code sets up a T5-small model for abstractive summarization by loading the CNN/DailyMail dataset, tokenizing it, and then fine-tuning the model using the Hugging Face Seq2SeqTrainer and ROUGE metrics.
